# Zpisher
# 1. Extract the file
unzip zphisher-master.zip

# 2. Enter the directory
cd zphisher-master

# 3. Grant execution permission
chmod +x zphisher.sh

# 4. Run the tool
./zphisher.sh

💡 Quick Tips:
​If you get a "command not found" for unzip, install it first using: pkg install unzip (for Termux) or sudo apt install unzip (for Linux).
​Make sure you are in the same folder where the .zip file is located before running the first command
@monaimFp
